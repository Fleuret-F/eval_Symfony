{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
{# <div>
    {{ article.id }}
    id
    {{ article.title }}
    title
    {{ article.auteur }}
    auteur
    {{ article.contenu }}</td>
    contenu
    {{ article.date ? article.date|date('Y-m-d H:i:s') : '' }}
    date
</div> #}


{% endblock %}